{
  "name": "ReconciliationEngineCollateralCorporateBanking",
  "version": "0.1.0",
  "private": false,
  "description": "Reconciliation Engine • Collateral (Corporate Banking) screens statements and monitors SARs; applies decision gates and observability; orchestrates via SWIFT Gateway to reduce fraud loss.",
  "main": "src/server/index.js",
  "keywords": [
    "ai-agent",
    "workflow",
    "automation",
    "banking & finance",
    "chain-of-thought",
    "agentbridge",
    "production"
  ],
  "author": "AgentBridge Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/<owner>/ReconciliationEngineCollateralCorporateBanking.git"
  },
  "bugs": {
    "url": "https://github.com/<owner>/ReconciliationEngineCollateralCorporateBanking/issues"
  },
  "homepage": "https://github.com/<owner>/ReconciliationEngineCollateralCorporateBanking#readme",
  "scripts": {
    "start": "node src/server/index.js",
    "dev": "nodemon src/server/index.js",
    "build": "npm run lint && npm run test",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src/ --ext .js,.ts",
    "lint:fix": "eslint src/ --ext .js,.ts --fix",
    "format": "prettier --write \"src/**/*.{js,ts,json}\"",
    "format:check": "prettier --check \"src/**/*.{js,ts,json}\"",
    "security:audit": "npm audit --audit-level moderate",
    "docker:build": "docker build -t ReconciliationEngineCollateralCorporateBanking .",
    "docker:run": "docker run -p 3000:3000 ReconciliationEngineCollateralCorporateBanking",
    "health:check": "node healthcheck.js",
    "pre-commit": "npm run lint && npm run test",
    "prepare": "husky install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.1",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "@types/node": "^20.10.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "lint-staged": {
    "*.{js,ts}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}